---
description: Review the current branch against main using AGENTS.md + REVIEW.md severity calibration.
argument-hint: '[optional file glob to scope the review]'
---

Follow the procedure in @.agents/commands/pr-review.md exactly. That file is the single source of truth, shared with Cursor and any other tool that grows a `pr-review` command.
