---
'noirium': minor
---

- `BaseBadge`: add `white`, `gray`, `black`, and `custom` (`customBackground` / `customText`) variants; export `BaseBadgeColor`.
- `Toast`: neutral panel and controls; type-specific colors on the status icon circle only.
