---
description: Add a new doc, agent rule, skill, or command using the project's classify-then-route flow.
---

Follow the procedure in @.agents/commands/add-doc.md exactly. That file is the single source of truth, shared with Claude Code and any other tool that grows an `add-doc` command.
